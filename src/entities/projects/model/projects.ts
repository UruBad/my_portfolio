import type { ProjectItem } from '@/shared/types/common';

// На первом этапе список может быть пустым. Структура предусмотрена для дальнейшего наполнения.
export const projects: ProjectItem[] = [];

